<script>
	
</script>

<section class="projects">
	<div class="projects-wrapper">
		<div class="header">
			<h1 class="title">Projects</h1>
		</div>
	</div>
</section>

<style>
	.projects {
		position: relative;
		font-size: var(--title-size);
		position: relative;
		width: 100%;
		min-height: 100vh;
		display: grid;
		align-items: center;
	}


	.projects .projects-wrapper {
		margin: 0 auto;
		width: 90%;
		height: 90vh;
	}

	.projects .header .title {
		display: inline;
		padding: 10px 25px;
		font-size: var(--title-size);
		border-bottom: var(--line-volumen) solid var(--primary);
		border-right: var(--line-volumen) solid var(--primary);
	}
</style>